#include <stdio.h>

int jumps (actual, i);

int main() {
  int stones[7], actual = 0, distance = 0, jumps;
  for (int i = 0; i < 7; i++) {
    scanf(" %i", &stones[i]);
    distance += stones{[i];
  }
  if (distance < 350) {
    for (int i = 0; i < 7; i++) {
      if ((stones[i] - actual) < )
    }
  }
  return 0;
}

int jumps (actual, i) {
  if ((actual + stone [(i + 1)] < 50) {
    return (jumps ([stone[i + 1], (i + 1)]))
  } else if ((actual + stone [(i + 1)]) == 50) {
    return (i + 1);
  } else if

}
